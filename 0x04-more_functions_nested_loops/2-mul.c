#include "main.h"


/**
 * mul - Multiplication two numbers
 * @x: first number of multiply
 * @y: second number of multiply
 * Return: The result of multiplication between x param and y param
 */

int mul(int x,int y)
{
	return (x * y);
}
