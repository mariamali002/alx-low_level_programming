#include <stdio.h>

/**
 * main -  is a method that print the name
 * @argv: ia argument of value
 * @argc: is argument of counter
 * Return: 0 Always success
 */
int main (int argc, char *argv[])
{
	printf("%s\n",argv[argc-argc]);
       return (0);
}       
